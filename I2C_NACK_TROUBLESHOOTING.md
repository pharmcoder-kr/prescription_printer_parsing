# ESP32 I2C NACK 오류 원인 및 대응 (로드셀 미연결 시)

## 증상 요약
- 로드셀(I2C 장치)이 **연결되지 않은 상태**에서 `I2C transaction failed`, `I2C hardware NACK detected` 발생
- 오류 발생 **빈도가 랜덤**
- **VIN(5V 외부 전원)** 사용 시 USB보다 **NACK 빈도가 더 높음**

---

## 1. 왜 NACK가 나오는가?

### 1.1 슬레이브가 없을 때
- I2C는 **주소를 보내면 해당 주소의 슬레이브가 ACK를 보내는 구조**입니다.
- **로드셀(또는 I2C ADC/저울 모듈)이 연결되지 않으면** 해당 주소에 응답할 장치가 없어서 **NACK**가 발생합니다.
- 따라서 “연결 안 됨” 상태에서 NACK 자체는 **정상적인 동작**에 가깝습니다.

### 1.2 빈도가 “랜덤”인 이유
- **타이밍**: 마스터가 트랜잭션을 시작하는 시점과, 떠있는 SDA/SCL 라인의 상태가 매번 조금씩 다름
- **풀업/플로팅**: 슬레이브가 없으면 SDA가 풀업만으로 풀업되어 있어서, 노이즈·리플에 따라 **가끔 ACK처럼 보이거나 NACK처럼 보이는** 경계 케이스 발생
- **전원/접지 불안정**: 전원 리플, 접지 노이즈가 크면 그때마다 NACK가 더 자주 또는 덜 나올 수 있음

---

## 2. VIN 사용 시 NACK가 더 자주 나는 이유

### 2.1 전원 품질 차이
- **USB**: PC/충전기 내부 레귤레이터 + 케이블로 5V가 공급되며, 상대적으로 **리플이 적고 안정적**인 경우가 많음.
- **VIN(외부 5V)**: 어댑터 종류에 따라 **리플, 노이즈, 부하 변동 시 전압 떨림**이 클 수 있음.

### 2.2 ESP32 내부 동작
- ESP32는 **3.3V 레귤레이터**로 VIN 5V를 3.3V로 낮춤.
- VIN 전원이 불안정하면:
  - **3.3V 레일이 순간적으로 떨어지거나** 노이즈가 타면
  - **I2C 클럭/데이터 타이밍**이 흔들리거나
  - **내부 I2C 블록 전원**이 불안정해져서
  → NACK 판별이 더 불안정해지거나, 트랜잭션 실패가 더 자주 발생할 수 있습니다.

### 2.3 I2C 라인과 전원의 관계
- 슬레이브가 없을 때 SDA/SCL은 **풀업 저항만**으로 풀업됨.
- 전원 리플이 크면:
  - **풀업 전압(3.3V)이 잠깐 떨어졌다 올라가는** 구간에서
  - 마스터가 “ACK/NACK”를 읽는 시점이 조금만 어긋나도 **NACK로 해석**될 수 있음.
- VIN이 불안정할수록 이런 구간이 더 자주 생겨 **NACK 빈도가 올라가는 것**으로 설명할 수 있습니다.

---

## 3. 대응 방법

### 3.1 하드웨어 (권장 순서)

1. **VIN 전원 품질**
   - **LDO 또는 저리플 DC-DC** 사용 (품질 좋은 5V 어댑터)
   - ESP32 **VIN–GND 근처에 100nF 세라믹 커패시터** 추가 (가능하면 10µF 전해/세라믹 병렬)
   - 긴 전원 케이블이면 **보드 바로 옆**에 커패시터 배치

2. **I2C 풀업**
   - SDA, SCL 각각 **2.2kΩ ~ 4.7kΩ** 풀업 (이미 있으면 값 확인)
   - 슬레이브가 없을 때도 **라인이 명확히 풀업**되도록 해서 노이즈에 덜 흔들리게 함

3. **배선**
   - I2C 선 **짧게**, 가능하면 **꼬임선/실드** 사용
   - **GND 공통**: ESP32 GND와 외부 5V 어댑터 GND가 한 점에서 잘 묶여 있는지 확인

4. **로드셀 모듈 전원**
   - 로드셀이 나중에 연결될 경우, **로드셀 보드에도 0.1µF 디커플링** 권장 (같은 3.3V 레일 사용 시)

### 3.2 소프트웨어 (펌웨어 쪽)

1. **재시도 + 타임아웃**
   - I2C 전송 실패 시 **2~3회 제한 재시도**
   - 일정 횟수 실패하면 “로드셀 미연결”로 간주하고, **해당 주기 동안 재요청 중단** (불필요한 트랜잭션 줄이기)

2. **폴링 주기 완화**
   - 로드셀이 없을 때 **연속으로 주소만 찔러보는 폴링**을 너무 짧은 주기로 하지 않기 (예: 100ms~1s 간격으로 “연결 확인”만 수행)

3. **초기화 시 프로브**
   - 부팅 시 **한두 번만** I2C 주소 프로브해서 “연결됨/미연결” 플래그 설정
   - 미연결이면 **무게 읽기 루프 자체를 돌리지 않음** → NACK 로그 대폭 감소

4. **에러 로그 레벨**
   - “로드셀 미연결”이 정상 시나리오라면, 해당 조건에서 **NACK를 에러가 아닌 “미연결” 정보 로그**로만 남기고, 반복 로그는 억제

---

## 4. 정리

| 구분 | 내용 |
|------|------|
| **NACK 원인** | 로드셀(슬레이브) 미연결 → 해당 주소에 ACK할 장치 없음 → NACK 정상 |
| **빈도가 랜덤인 이유** | 타이밍, 풀업/플로팅 상태, 전원·접지 노이즈에 따른 감도 차이 |
| **VIN에서 더 자주 나는 이유** | USB보다 전원 리플/불안정 가능성 큼 → 3.3V·I2C 라인 흔들림 → NACK 판별·트랜잭션 실패 증가 |
| **우선 조치** | VIN 근처 디커플링, I2C 풀업 확인, GND 공통·배선 정리 + 소프트웨어에서 “미연결” 시 재시도/폴링 최소화 |

로드셀을 연결하면 해당 슬레이브가 ACK를 보내므로 NACK는 사라져야 합니다. **연결 안 된 상태에서 NACK를 줄이려면** 전원 안정화(특히 VIN)와 “미연결일 때는 I2C 트랜잭션을 최소화하는” 소프트웨어 처리가 가장 효과적입니다.
